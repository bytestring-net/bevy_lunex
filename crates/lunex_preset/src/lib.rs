pub mod prelude {
    
}

pub mod interactive {
    pub struct Button;
    pub struct Toggle;
    pub struct Selector;
    pub struct RangeSlider;
    pub struct Radio;
}

pub mod predefined {
    pub struct Badge;
    pub struct Chip;
    pub struct Avatar;
    pub struct ProgressBar;
    pub struct ProgressRadial;
    pub struct Tabs;
    pub struct Popup;
    pub struct Modal;
    pub struct Toast;
    pub struct Drawer;
}